---
description: Memory | Search CTX memory directives by topic
---

Search CTX graph memory for a specific topic.

Arguments:
- `$1`: required search query
- `$2`: optional scope
- `$3`: optional limit

Run the matching `'/Users/honey/.local/bin/ctx' --repo-root '/Users/honey/Documents/libran/libran' memory search "$1" --json` command, adding scope and limit only when they were provided.
Show only the matching directives in a compact, predictable format.
Do not add extra commentary beyond one short sentence if needed.
